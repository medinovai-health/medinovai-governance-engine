---
description: Git tag checkpoint before destructive or schema changes
---
Create `git tag harness-checkpoint-session-$(date +%Y%m%d%H%M)-pre-change -m "pre-change"` and record the tag name in `claude-progress.txt`.
