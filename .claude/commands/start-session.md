---
description: Tier 1 session start — verify harness state and pick one feature
---
Run `bash init.sh`. Read `claude-progress.txt` and `feature_list.json`. Run `git log --oneline -20`.
Select exactly **one** feature with `"passes": false` (highest priority P1 first).
State the feature id, regulatory tags, and your implementation plan. Do not start a second feature.
